---
phase: 10-skill-capability-enhancements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-super-team/skills/build/references/finalization-guide.md
  - plugins/claude-super-team/skills/build/SKILL.md
autonomous: true

must_haves:
  truths:
    - "/build runs a completion audit after Step 12 (BUILD-REPORT.md generation) that scans for failed verifications, incomplete phases, and unresolved issues"
    - "The audit is bounded: max 2 remediation cycles using /phase-feedback, then stop regardless of outcome"
    - "Audit findings are recorded in BUILD-STATE.md for compaction resilience"
    - "If the audit finds no gaps, the pipeline proceeds directly to the completion summary (Step 13) with zero overhead"
    - "/build's core pipeline flow (Steps 1-12) is unchanged"
  artifacts:
    - "plugins/claude-super-team/skills/build/references/finalization-guide.md with new Step 12.5 completion audit section"
    - "plugins/claude-super-team/skills/build/SKILL.md with updated Steps 10-13 description mentioning the audit"
  key_links:
    - "Finalization guide Step 12.5 sits between BUILD-REPORT.md generation (Step 12) and completion summary (Step 13)"
    - "Step 12.5 references BUILD-STATE.md tables (Phase Progress, Validation Results, Incomplete Phases, Errors) as its scan inputs"
    - "BUILD-STATE.md Pipeline Progress table gets a new 'completion-audit' row tracked by /build"
---

<objective>
Add a completion audit step to /build's finalization that detects remaining work (failed verifications, incomplete phases, new requirements) and autonomously remediates bounded gaps before generating the final report.

Purpose: /build should be dynamic and know when there is more to do rather than stopping at a fixed pipeline endpoint. The audit makes /build the single entry point for autonomous work that delivers completeness, not just pipeline traversal.
Output: Modified finalization-guide.md with audit step, updated SKILL.md description referencing the audit
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-skill-capability-enhancements/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add completion audit step to finalization-guide.md</name>
  <files>plugins/claude-super-team/skills/build/references/finalization-guide.md</files>
  <action>
  Insert a new section "### Step 12.5: Completion Audit" between the existing Step 12 (Generate BUILD-REPORT.md) and Step 13 (Present Completion Summary). This restructures the finalization flow to: Step 10 -> Step 11 -> Step 12 -> **Step 12.5** -> Step 13.

  The Step 12.5 content should be:

  **Position:** After the Step 12 closing `---` line and before the `### Step 13` heading.

  **Section content (prose instructions, not code dumps):**

  ```markdown
  ### Step 12.5: Completion Audit

  Scan for remaining work that the pipeline missed or could not resolve, then autonomously remediate what is possible within bounded limits.

  Update BUILD-STATE.md: set Current stage to `completion-audit`. Add a Pipeline Progress row for `completion-audit` with status `in_progress`.

  #### 12.5a. Gap Detection

  Read BUILD-STATE.md and scan for gaps in these categories:

  **Category 1 -- Incomplete Phases:** Check the Phase Progress table for any phase where Status is `incomplete` or Validate is `failed`. These are phases where execution or validation did not fully succeed.

  **Category 2 -- Failed Verifications:** Check the Validation Results table for any row where Final Status is `incomplete` or `fail`. Cross-reference with VERIFICATION.md files in phase directories -- look for `status: gaps_found` or similar failure indicators.

  **Category 3 -- Unresolved Errors:** Check the Errors section of BUILD-STATE.md for logged errors that were not resolved by auto-fix (Step 11). Errors with no corresponding fix attempt are candidates.

  **Category 4 -- Build/Test Failures Still Present:** If Step 10/11 final validation ended with status `partial` (auto-fix failed after 3 attempts), the build or tests still fail.

  Compile a gap list with: gap category, phase number (if applicable), brief description.

  **If no gaps found:** Print `Completion audit: no gaps detected.` Set Pipeline Progress `completion-audit` to `complete`. Skip to Step 13.

  **If gaps found:** Print `Completion audit: {N} gap(s) detected. Starting remediation.` Continue to 12.5b.

  #### 12.5b. Bounded Remediation (Max 2 Cycles)

  For cycle 1 to 2:

  1. **Prioritize gaps:** Sort by impact -- build/test failures first, then incomplete phases, then failed verifications, then unresolved errors.

  2. **For each gap (in priority order):**

     **Build/test failures (Category 4):** Run one more targeted auto-fix attempt using the same approach as Step 11 (analyze errors, apply targeted fixes, re-run build+test). This gives a total of up to 5 auto-fix attempts across the pipeline (3 from Step 11 + 2 from audit).

     **Incomplete phases (Category 1):** Invoke `/phase-feedback` for the phase with a synthesized error description from the Errors section or Validation Results. Answer all AskUserQuestion calls autonomously per the decision guide. If /phase-feedback creates a subphase, invoke `/execute-phase` for it. Commit results.

     **Failed verifications (Category 2):** Invoke `/phase-feedback` for the phase, describing the verification gap. Same autonomous handling as above.

     **Unresolved errors (Category 3):** Attempt a direct fix using Edit/Write tools if the error is clear and localized. If the error is architectural or unclear, log it as unresolvable and skip.

  3. **After processing all gaps in this cycle:** Re-run build + test validation (same detection logic as Step 10). Record results.

  4. **If all gaps resolved and build+tests pass:** Print `Completion audit cycle {N}: all gaps resolved.` Break out of loop.

  5. **If gaps remain:** Log remaining gaps in BUILD-STATE.md Errors section. Continue to next cycle.

  **After 2 cycles (or earlier if all gaps resolved):**

  Update BUILD-REPORT.md with audit results. Add a new section "## Completion Audit" to BUILD-REPORT.md (before the "Next Steps" section) containing:
  - Gaps detected (count and categories)
  - Remediation attempts (count and outcomes)
  - Remaining unresolved gaps (if any)

  Update BUILD-STATE.md:
  - Set Pipeline Progress `completion-audit` to `complete`.
  - If audit resolved all gaps and build status was `partial`, upgrade Status to `complete`.
  - If gaps remain, keep Status as-is (`partial` or `failed`).

  Print: `Completion audit finished. {resolved}/{total} gaps resolved.`
  ```

  **Also update Step 12** slightly: after the line that says `Set Current stage to `done`.`, change it to `Set Current stage to `completion-audit`.` instead. The `done` stage is now set at the end of Step 12.5, not Step 12. Similarly, in Step 12 where it sets Status to `complete`/`partial`/`failed`, change this to set Status to `audit-pending` as a temporary value -- the final status is determined after the audit in Step 12.5. Specifically:

  - In Step 12, change "Set Status to `complete` (or `partial` or `failed` based on outcome)." to "Set Status to the pre-audit value (`complete`, `partial`, or `failed` based on outcome so far). The completion audit (Step 12.5) may upgrade `partial` to `complete` if it resolves all gaps."
  - In Step 12, change "Set Current stage to `done`." to "Set Current stage to `completion-audit`."

  **Update the success criteria checklist** at the bottom of finalization-guide.md to add:
  ```
  - [ ] Completion audit ran and scanned for gaps after BUILD-REPORT.md generation
  - [ ] Audit remediation bounded to max 2 cycles
  - [ ] Audit results recorded in BUILD-REPORT.md
  ```
  </action>
  <verify>
  Read finalization-guide.md and confirm:
  1. Step 12.5 exists between Step 12 and Step 13
  2. Step 12.5 has gap detection (4 categories) and bounded remediation (max 2 cycles)
  3. Step 12 no longer sets Current stage to `done` (now sets `completion-audit`)
  4. Step 12 status assignment is updated to note the audit may upgrade status
  5. Success criteria checklist includes audit items
  6. Steps 10, 11, 13 are unchanged in their core logic
  </verify>
  <done>finalization-guide.md contains Step 12.5 with gap detection, bounded remediation, and BUILD-REPORT.md audit section generation. Step 12 correctly hands off to the audit before Step 13.</done>
</task>

<task type="auto">
  <name>Task 2: Update build SKILL.md to reference the completion audit</name>
  <files>plugins/claude-super-team/skills/build/SKILL.md</files>
  <action>
  Make two targeted edits to SKILL.md:

  **Edit 1 -- Update Steps 10-13 description (around line 483-497):** The current text says the finalization guide covers Steps 10-13. Update the bullet list to include the audit:

  Change:
  ```
  Follow the finalization guide. It covers:
  - Step 10: Final validation (always run build + tests after all phases)
  - Step 11: Auto-fix loop (max 3 attempts on failure)
  - Step 12: Generate BUILD-REPORT.md from BUILD-STATE.md data
  - Step 13: Present completion summary to user
  - Success criteria checklist
  ```

  To:
  ```
  Follow the finalization guide. It covers:
  - Step 10: Final validation (always run build + tests after all phases)
  - Step 11: Auto-fix loop (max 3 attempts on failure)
  - Step 12: Generate BUILD-REPORT.md from BUILD-STATE.md data
  - Step 12.5: Completion audit (scan for gaps, bounded remediation -- max 2 cycles)
  - Step 13: Present completion summary to user
  - Success criteria checklist
  ```

  **Edit 2 -- Update the Pipeline description (around line 67):** The current pipeline summary does not mention the audit. Update it:

  Change:
  ```
  **Pipeline:** Input detection -> /new-project -> [/map-codebase] -> /brainstorm -> /create-roadmap -> For each sprint: { discuss/research all phases -> plan all phases (parallel) -> execute all phases (branch per phase) -> validate + merge each -> sprint boundary validation } -> Final validation -> [auto-fix] -> BUILD-REPORT.md
  ```

  To:
  ```
  **Pipeline:** Input detection -> /new-project -> [/map-codebase] -> /brainstorm -> /create-roadmap -> For each sprint: { discuss/research all phases -> plan all phases (parallel) -> execute all phases (branch per phase) -> validate + merge each -> sprint boundary validation } -> Final validation -> [auto-fix] -> BUILD-REPORT.md -> [completion audit] -> Summary
  ```

  **Edit 3 -- Update Key behaviors (around line 72):** Add a bullet for the audit:

  After the line `- 3-attempt auto-fix loop on final build/test failure`, add:
  ```
  - Completion audit: scans for gaps after report generation, bounded remediation (max 2 cycles)
  ```
  </action>
  <verify>
  Read SKILL.md and confirm:
  1. Steps 10-13 bullet list includes "Step 12.5: Completion audit"
  2. Pipeline description includes "[completion audit]"
  3. Key behaviors section includes the audit bullet
  4. No other content in SKILL.md was changed (Steps 0-9, resume logic, extend mode, etc.)
  </verify>
  <done>SKILL.md references the completion audit in the pipeline description, key behaviors, and finalization steps list. The core pipeline flow (Steps 0-9) is unchanged.</done>
</task>

</tasks>

<verification>
1. Read finalization-guide.md to confirm Step 12.5 is correctly positioned and complete
2. Read SKILL.md to confirm all three edits were applied and no existing content was broken
3. Verify the audit's bounded nature: max 2 remediation cycles with clear termination
4. Verify gap detection covers all 4 categories: incomplete phases, failed verifications, unresolved errors, build/test failures
5. Confirm BUILD-REPORT.md gets a "Completion Audit" section from the audit results
</verification>

<success_criteria>
/build's finalization includes a completion audit (Step 12.5) that scans for gaps across 4 categories, runs bounded remediation (max 2 cycles), records results in BUILD-STATE.md and BUILD-REPORT.md, and can upgrade status from `partial` to `complete` if all gaps are resolved. The core pipeline (Steps 0-12) is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-capability-enhancements/10-02-SUMMARY.md`
</output>
