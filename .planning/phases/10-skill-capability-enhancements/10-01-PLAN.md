---
phase: 10-skill-capability-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-super-team/skills/cst-help/SKILL.md
  - plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
  - plugins/claude-super-team/skills/cst-help/references/troubleshooting.md
autonomous: true

must_haves:
  truths:
    - "Running /cst-help explain .planning/phases/01-foundation/01-01-PLAN.md produces a 5-10 sentence narrative explaining the artifact's purpose, constraints, and connections"
    - "The explain routing detects 'explain' keyword plus a .planning/ file path in $ARGUMENTS and routes to the explain response handler"
    - "The explain handler reads the target file, its phase CONTEXT.md, RESEARCH.md, and the ROADMAP.md phase detail section"
    - "Non-explain queries still route to the existing help, troubleshoot, skill reference, and what-to-do-next handlers unchanged"
  artifacts:
    - "plugins/claude-super-team/skills/cst-help/SKILL.md with new explain routing case and response section"
    - "plugins/claude-super-team/skills/cst-help/references/workflow-guide.md with explain capability documented"
    - "plugins/claude-super-team/skills/cst-help/references/troubleshooting.md with explain troubleshooting entry and updated skill usage section"
  key_links:
    - "Phase 1 (Classify Request) in SKILL.md routes 'explain' intent to the new 'Help Response: Explain Artifact' section"
    - "Skill Reference output in SKILL.md mentions the explain capability for /cst-help"
---

<objective>
Add an "explain" capability to /cst-help that reads a .planning/ artifact and its surrounding context to produce a concise narrative explaining its purpose, constraints, and connections.

Purpose: Users need a quick way to understand what a planning artifact means and why it exists without reading multiple files. This closes the gap between raw artifact files and the reasoning chain behind them.
Output: Modified SKILL.md with explain routing + response section, updated workflow-guide.md and troubleshooting.md
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-skill-capability-enhancements/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explain routing and response handler to cst-help SKILL.md</name>
  <files>plugins/claude-super-team/skills/cst-help/SKILL.md</files>
  <action>
  Make three targeted edits to the existing SKILL.md:

  **Edit 1 -- Classify Request (Phase 1, around line 54):** Add an "explain" classification branch. After the line "**If $ARGUMENTS contains a specific question:**" and before "Classify as general vs project-specific", add a new branch:

  ```
  **If $ARGUMENTS starts with "explain" followed by a file path (contains `.planning/`):**

  Route directly to "Help Response: Explain Artifact" below. Skip Phase 2.
  ```

  The detection heuristic: split $ARGUMENTS on whitespace. If the first token is "explain" (case-insensitive) and any subsequent token contains `.planning/`, treat as an explain request. The path token is the `$TARGET_PATH`. If no `.planning/` path is found after "explain", ask the user which artifact they want explained using AskUserQuestion.

  **Edit 2 -- New response section:** Add a new section "## Help Response: Explain Artifact" after the existing "## Help Response: Understand Concepts" section (after the closing `---` around line 269). The section content:

  ```markdown
  ## Help Response: Explain Artifact

  **Goal:** Explain what a .planning/ artifact means, why it exists, and how it connects to the project.

  **Step 1: Read the target file**

  Read the file at $TARGET_PATH. If the file does not exist, report the error and suggest the user check the path.

  **Step 2: Gather surrounding context**

  Determine which phase the artifact belongs to by parsing its path. A file at `.planning/phases/NN-name/...` belongs to phase NN.

  Read these context files (skip any that do not exist):
  - The phase's CONTEXT.md: `.planning/phases/{NN}-{name}/{NN}-CONTEXT.md` (decisions and constraints)
  - The phase's RESEARCH.md: `.planning/phases/{NN}-{name}/{NN}-RESEARCH.md` (ecosystem findings)
  - The ROADMAP.md phase detail section for phase NN: extract the `### Phase {N}: {name}` block from `.planning/ROADMAP.md`

  For files directly under `.planning/` (PROJECT.md, ROADMAP.md, STATE.md, IDEAS.md), there is no phase context. Read only the target file itself.

  **Step 3: Synthesize explanation**

  Produce a concise narrative (5-10 sentences) that answers:
  1. What is this artifact? (type: plan, summary, context, research, verification, etc.)
  2. Why does it exist? (what phase goal or decision motivated it)
  3. What are its key constraints or decisions? (from CONTEXT.md or RESEARCH.md)
  4. How does it connect to the broader project? (which phase, what comes before/after)

  **Output format:**

  ```
  **{filename}** -- {artifact type} for Phase {N}: {phase name}

  {5-10 sentence narrative}

  Related files:
  - {list paths of context files that were read, if any}
  ```

  For non-phase files (PROJECT.md, ROADMAP.md, etc.), omit the phase reference and related files list.
  ```

  **Edit 3 -- Update Skill Reference section:** In the "Help Response: Skill Reference" section, update the /cst-help entry (around line 467) to mention the explain capability:

  Change:
  ```
  /cst-help [question]
    This skill -- context-aware help and troubleshooting
  ```

  To:
  ```
  /cst-help [question]
    Context-aware help, troubleshooting, and artifact explanation
    Explain mode: /cst-help explain .planning/path/to/artifact.md
  ```
  </action>
  <verify>
  Read the modified SKILL.md and confirm:
  1. Phase 1 has an "explain" classification branch that detects "explain" + `.planning/` path
  2. A "Help Response: Explain Artifact" section exists with Steps 1-3
  3. The Skill Reference section mentions explain mode
  4. All existing sections (General Workflow Question, What to Do Next, Understand Concepts, Troubleshoot, Skill Reference) are unchanged in their content
  </verify>
  <done>SKILL.md contains explain routing, explain response handler, and updated skill reference. All existing routing paths are unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Update cst-help reference documents</name>
  <files>
  plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
  plugins/claude-super-team/skills/cst-help/references/troubleshooting.md
  </files>
  <action>
  **workflow-guide.md:** In the "Help" section at the bottom of the "Sequential Pipeline" or the file structure section, update the /cst-help entry to mention explain:

  Find the section where /cst-help is mentioned and add a note that it supports `explain [path]` for artifact explanation.

  **troubleshooting.md -- Add troubleshooting entry:** Add a new subsection under an appropriate heading (near "### Build Automation" or at the end before "## Diagnostic Commands"):

  ```markdown
  ### Artifact Explanation

  #### "Explain doesn't find the file"

  **Symptom:** `/cst-help explain path/to/file` returns "file not found"

  **Cause:** Path must be relative to project root and start with `.planning/`

  **Solution:**
  - Use the full relative path: `/cst-help explain .planning/phases/01-foundation/01-01-PLAN.md`
  - Check path with `ls .planning/phases/` if unsure

  #### "Explanation is too vague"

  **Symptom:** Explain output lacks specific reasoning

  **Cause:** Phase context files (CONTEXT.md, RESEARCH.md) may not exist for that phase

  **Solution:**
  - Run `/discuss-phase N` and/or `/research-phase N` to create context
  - More context files = richer explanation
  ```

  **troubleshooting.md -- Update "When to Use Each Skill" section:** Find the `/cst-help` entry in the "When to Use Each Skill" section (it may not have a dedicated subsection). If absent, add one:

  ```markdown
  ### Use `/cst-help` when:
  - Need help understanding the workflow or a specific skill
  - Want to know what to do next in your project
  - Troubleshooting an issue with a skill
  - Want to understand what a .planning/ artifact means: `/cst-help explain .planning/path`
  ```

  If the section already exists, append the explain bullet point.
  </action>
  <verify>
  1. Read workflow-guide.md and confirm /cst-help mention includes explain capability
  2. Read troubleshooting.md and confirm: (a) "Artifact Explanation" troubleshooting section exists, (b) "When to Use Each Skill" includes /cst-help with explain mention
  </verify>
  <done>Both reference documents mention the explain capability. Troubleshooting guide has entries for common explain issues. "When to Use Each Skill" includes /cst-help with explain use case.</done>
</task>

</tasks>

<verification>
1. Read SKILL.md end-to-end to confirm no existing sections were broken
2. Verify the explain routing in Phase 1 correctly distinguishes "explain .planning/..." from other query types
3. Confirm the explain response section reads the right context files (CONTEXT.md, RESEARCH.md, ROADMAP.md phase block)
4. Verify Skill Reference section mentions explain mode
5. Confirm workflow-guide.md and troubleshooting.md are updated
</verification>

<success_criteria>
/cst-help SKILL.md contains an explain routing case that detects "explain" + .planning/ path, reads the target artifact plus phase context, and produces a 5-10 sentence narrative. Reference documents are updated. All existing routing paths remain functional.
</success_criteria>

<output>
After completion, create `.planning/phases/10-skill-capability-enhancements/10-01-SUMMARY.md`
</output>
