---
phase: 09-script-consolidation-state-compaction
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - plugins/claude-super-team/skills/build/gather-data.sh
  - plugins/claude-super-team/skills/code/gather-data.sh
  - plugins/claude-super-team/skills/create-roadmap/gather-data.sh
  - plugins/claude-super-team/skills/discuss-phase/gather-data.sh
  - plugins/claude-super-team/skills/execute-phase/gather-data.sh
  - plugins/claude-super-team/skills/phase-feedback/gather-data.sh
  - plugins/claude-super-team/skills/plan-phase/gather-data.sh
  - plugins/claude-super-team/skills/progress/gather-data.sh
  - plugins/claude-super-team/skills/research-phase/gather-data.sh
autonomous: true

must_haves:
  truths:
    - "All 9 active gather-data.sh scripts source gather-common.sh and call its functions instead of inlining the logic"
    - "Output format of every gather-data.sh script is identical before and after refactoring"
    - "Per-skill custom sections remain untouched in each gather-data.sh"
    - "SKIP_PROJECT, SKIP_ROADMAP, SKIP_STATE env var behavior is preserved"
  artifacts:
    - "9 updated gather-data.sh files"
  key_links:
    - "Every gather-data.sh sources gather-common.sh via CLAUDE_PLUGIN_ROOT-relative path"
    - "Custom per-skill sections remain inline in each gather-data.sh"
---

<objective>
Migrate all 9 active gather-data.sh scripts to source gather-common.sh for shared sections while preserving their skill-specific sections and exact output format.

Purpose: Eliminates duplicated logic across 9 scripts, making future changes to shared patterns a single-file edit.
Output: 9 updated gather-data.sh scripts that produce identical output to their pre-refactoring versions.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate high-overlap gather scripts (build, execute-phase, progress)</name>
  <files>
    plugins/claude-super-team/skills/build/gather-data.sh
    plugins/claude-super-team/skills/execute-phase/gather-data.sh
    plugins/claude-super-team/skills/progress/gather-data.sh
  </files>
  <action>
These 3 scripts have the most overlap with gather-common.sh (PROJECT/ROADMAP/STATE emission, PHASE_COMPLETION, SYNC_CHECK, PREFERENCES). Migrate them first.

For each script, add a source line near the top:
```bash
source "${CLAUDE_PLUGIN_ROOT}/scripts/gather-common.sh"
```

**build/gather-data.sh**: Replace the `=== PROJECT ===` section (lines 47-64) with calls to `emit_project_section` (with embedded cat + HAS_PROJECT/HAS_ROADMAP/HAS_STATE logic), and the `=== PHASE_COMPLETION ===` section (lines 67-84) with `emit_phase_completion`. Keep the BUILD_STATE, PREFERENCES (build has its own preferences pattern -- global/project prefs, not STATE.md prefs), GIT, EXTEND, and BROWNFIELD sections as-is since they are skill-specific.

**execute-phase/gather-data.sh**: Replace PROJECT/ROADMAP/STATE emission (lines 4-15) with `emit_project_section`, `emit_roadmap_section`, `emit_state_section`. Replace PREFERENCES (lines 18-22) with `emit_preferences`. Replace PHASE_COMPLETION (lines 45-62) with `emit_phase_completion`. Replace ROADMAP_CHECKED (lines 64-71) with the relevant portion of `emit_sync_check` (or just the CHECKED/UNCHECKED extraction). Keep PHASE_PLANS and GIT sections as skill-specific.

**progress/gather-data.sh**: Replace PROJECT/ROADMAP/STATE emission (lines 7-18) with calls to shared functions. Replace STRUCTURE (lines 59-64) with `emit_structure`. Replace PHASE_MAP (lines 67-79) with the shared function (note: PHASE_MAP output format matches what emit_phase_completion already covers for phase iteration -- but PHASE_MAP includes gaps/context/research counts that PHASE_COMPLETION does not; keep PHASE_MAP as skill-specific unless gather-common.sh's format matches). Replace SYNC_CHECK (lines 92-111) with `emit_sync_check`. Keep DEPENDENCIES, RECENT_SUMMARIES, BUILD, and GIT as skill-specific.

CRITICAL constraint: The output of each script MUST NOT change. If gather-common.sh emits a slightly different format for a section than the script currently uses, keep that section inline and do not migrate it. Output compatibility is more important than deduplication.
  </action>
  <verify>
For each modified script, capture output before and after:
```bash
# Before modifying (compare against git stash or saved copy):
cd /path/to/project && bash plugins/claude-super-team/skills/build/gather-data.sh 2>/dev/null > /tmp/build-before.txt
# After modifying:
bash plugins/claude-super-team/skills/build/gather-data.sh 2>/dev/null > /tmp/build-after.txt
diff /tmp/build-before.txt /tmp/build-after.txt
```

Run `bash -n` on each modified script to verify no syntax errors.
  </verify>
  <done>build, execute-phase, and progress gather-data.sh scripts source gather-common.sh, call shared functions for duplicated sections, retain skill-specific sections inline, and produce identical output.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining 6 gather scripts</name>
  <files>
    plugins/claude-super-team/skills/code/gather-data.sh
    plugins/claude-super-team/skills/create-roadmap/gather-data.sh
    plugins/claude-super-team/skills/discuss-phase/gather-data.sh
    plugins/claude-super-team/skills/phase-feedback/gather-data.sh
    plugins/claude-super-team/skills/plan-phase/gather-data.sh
    plugins/claude-super-team/skills/research-phase/gather-data.sh
  </files>
  <action>
These 6 scripts share primarily the PROJECT/ROADMAP/STATE emission pattern. Migrate each:

Add `source "${CLAUDE_PLUGIN_ROOT}/scripts/gather-common.sh"` near the top of each script.

**code/gather-data.sh**: Replace the 3 PROJECT/ROADMAP/STATE sections (lines 8-19) with `emit_project_section`, `emit_roadmap_section`, `emit_state_section`. Keep EXECUTED_PHASES, CURRENT_PHASE, RECENT_SESSIONS as skill-specific.

**create-roadmap/gather-data.sh**: Replace PROJECT/ROADMAP emission (lines 4-11) with `emit_project_section`, `emit_roadmap_section`. Keep STRUCTURE, EXISTING_PHASES, HIGHEST_PHASE, DECIMAL_PHASES as skill-specific. Note: create-roadmap's STRUCTURE section checks for HAS_REQUIREMENTS and HAS_RESEARCH which the shared emit_structure may not cover -- if so, keep the extra checks inline after calling emit_structure.

**discuss-phase/gather-data.sh**: Replace PROJECT/ROADMAP emission (lines 4-11) with shared functions. Keep PHASE_ARTIFACTS and CODEBASE_DOCS as skill-specific.

**phase-feedback/gather-data.sh**: Replace PROJECT/ROADMAP/STATE emission (lines 4-15) with shared functions. Keep EXECUTED_PHASES, CURRENT_PHASE, SUBPHASES as skill-specific.

**plan-phase/gather-data.sh**: Replace PROJECT/ROADMAP/STATE emission (lines 12-24) with shared functions. Keep PHASE_STATUS, ROADMAP_PHASES, ROADMAP_TRIMMED, STATE_TRIMMED, CODEBASE_DOCS, PHASE_CONTEXT, PHASE_RESEARCH, PHASE_REQUIREMENTS as skill-specific. Note: plan-phase has the most custom sections -- only the top 3 sections are candidates.

**research-phase/gather-data.sh**: Replace PROJECT/ROADMAP/STATE emission (lines 6-17) with shared functions. Replace PREREQUISITES (lines 20-23, which check HAS_PROJECT/HAS_ROADMAP/HAS_STATE) with `emit_structure` or inline checks. Keep PHASE_ARTIFACTS, ROADMAP_PHASES, CODEBASE_DOCS as skill-specific.

Same CRITICAL constraint: output must not change. If a shared function's format doesn't match, keep the section inline.
  </action>
  <verify>
Run `bash -n` on all 6 modified scripts.

For each, verify output compatibility:
```bash
bash plugins/claude-super-team/skills/code/gather-data.sh 2>/dev/null | head -20
bash plugins/claude-super-team/skills/plan-phase/gather-data.sh 2>/dev/null | head -20
```

Verify that the source line resolves correctly by checking the CLAUDE_PLUGIN_ROOT convention matches what skills set at runtime.
  </verify>
  <done>All 6 remaining gather-data.sh scripts source gather-common.sh, call shared functions for PROJECT/ROADMAP/STATE sections, and produce identical output to their pre-refactoring versions.</done>
</task>

</tasks>

<verification>
1. All 9 gather-data.sh files pass `bash -n` syntax check
2. Each script contains `source "${CLAUDE_PLUGIN_ROOT}/scripts/gather-common.sh"`
3. No script duplicates PROJECT/ROADMAP/STATE emission logic inline (they call shared functions)
4. Each script retains its skill-specific sections unchanged
5. Running any gather-data.sh from the project root produces the same output as before the refactoring
</verification>

<success_criteria>
All 9 active gather-data.sh scripts source gather-common.sh and call shared functions for PROJECT/ROADMAP/STATE emission (and PHASE_COMPLETION/SYNC_CHECK/PREFERENCES where applicable). No output format changes. Skill-specific sections remain inline.
</success_criteria>

<output>
After completion, create `.planning/phases/09-script-consolidation-state-compaction/09-02-SUMMARY.md`
</output>
