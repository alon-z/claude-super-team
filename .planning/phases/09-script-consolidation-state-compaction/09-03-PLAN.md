---
phase: 09-script-consolidation-state-compaction
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - plugins/claude-super-team/skills/discuss-phase/SKILL.md
  - plugins/claude-super-team/skills/plan-phase/SKILL.md
  - plugins/claude-super-team/skills/quick-plan/SKILL.md
  - plugins/claude-super-team/skills/execute-phase/SKILL.md
autonomous: true

must_haves:
  truths:
    - "All 4 SKILL.md files use create_phase_dir() from phase-utils.sh instead of inline grep/sed/mkdir pipelines"
    - "execute-phase compaction step (Phase 8) archives completed-phase decisions from STATE.md into a Decision Archive section"
    - "Gather scripts that read STATE.md stop reading at the Decision Archive delimiter"
  artifacts:
    - "4 updated SKILL.md files"
  key_links:
    - "create_phase_dir() in phase-utils.sh is the single source of truth for phase directory creation"
    - "Decision Archive delimiter (### Decision Archive) is respected by gather-common.sh emit_state_section"
---

<objective>
Replace inline phase directory creation pipelines in 4 SKILL.md files with create_phase_dir() calls, and extend execute-phase's state compaction to archive completed-phase decisions in STATE.md.

Purpose: Centralizes phase directory creation logic (single function to maintain) and keeps STATE.md lean by archiving old decisions.
Output: 4 updated SKILL.md files using the shared function, plus STATE.md decision archival in execute-phase.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline phase directory creation in 4 SKILL.md files</name>
  <files>
    plugins/claude-super-team/skills/discuss-phase/SKILL.md
    plugins/claude-super-team/skills/plan-phase/SKILL.md
    plugins/claude-super-team/skills/quick-plan/SKILL.md
  </files>
  <action>
In each of the 3 SKILL.md files listed below, find the inline phase directory creation pipeline and replace it with a call to `create_phase_dir()`.

**discuss-phase/SKILL.md** (Phase 1, lines ~54-61): Replace the block:
```bash
PHASE_DIR=$(ls -d .planning/phases/${PHASE}-* 2>/dev/null | head -1)
if [ -z "$PHASE_DIR" ]; then
  PHASE_NAME=$(grep "Phase ${PHASE_NUM}:" .planning/ROADMAP.md | sed 's/.*Phase [0-9]*: //' | sed 's/ *-.*//' | tr '[:upper:]' '[:lower:]' | tr ' ' '-')
  mkdir -p ".planning/phases/${PHASE}-${PHASE_NAME}"
  PHASE_DIR=".planning/phases/${PHASE}-${PHASE_NAME}"
fi
```
With:
```bash
source "${CLAUDE_PLUGIN_ROOT}/scripts/phase-utils.sh"
PHASE=$(normalize_phase "$PHASE_NUM")
PHASE_DIR=$(create_phase_dir "$PHASE_NUM")
```
Note: `source` and `normalize_phase` are already called earlier in the same step. Ensure the replacement doesn't duplicate the source/normalize calls -- just replace the directory creation block with the `create_phase_dir` call and use the returned path.

The simplest replacement is: keep the existing `source` and `PHASE=$(normalize_phase ...)` lines (already present), and replace the 6-line `PHASE_DIR` block with just:
```bash
PHASE_DIR=$(create_phase_dir "$PHASE_NUM")
```

**plan-phase/SKILL.md** (Phase 3, lines ~82-88): Same pattern. Replace the inline pipeline with:
```bash
PHASE_DIR=$(create_phase_dir "$PHASE_NUM")
```
Keep the existing source and normalize_phase calls already present in Phase 2.

**quick-plan/SKILL.md** (Step 4, lines ~93-97): This skill builds its own slug from $DESCRIPTION rather than from ROADMAP.md. It also uses decimal numbering (`${QUICK_PHASE_PADDED}-${slug}`). The create_phase_dir() function derives the name from ROADMAP.md, but quick-plan adds its ROADMAP annotation BEFORE creating the directory (Step 5 before Step 4 in actual execution flow -- but reading the skill, Step 5 annotates ROADMAP.md first, then Step 4 creates the dir).

Actually, re-reading quick-plan: Step 3 determines the number, Step 4 creates the directory, Step 5 annotates ROADMAP.md. So the directory is created BEFORE the ROADMAP annotation. This means create_phase_dir() cannot find the phase in ROADMAP.md yet.

Resolution: For quick-plan, restructure the instructions to annotate ROADMAP.md first (Step 5) then call create_phase_dir() (Step 4). Or alternatively, use the slug from $DESCRIPTION as a fallback in create_phase_dir(). The simplest approach: swap the order in quick-plan's SKILL.md so Step 5 (Annotate ROADMAP.md) comes before Step 4 (Create Phase Directory), then replace Step 4's mkdir with `PHASE_DIR=$(create_phase_dir "$QUICK_PHASE")`. Update the step numbering accordingly in the instructions.

If reordering feels too invasive, the alternative is to keep quick-plan's inline directory creation as-is (it genuinely has different logic -- slug from description, not ROADMAP). Document this exception in a brief comment.

Prefer the pragmatic option: keep quick-plan's inline creation since it derives the slug from user description, not ROADMAP.md. This is fundamentally different behavior. Just add a comment explaining why it doesn't use create_phase_dir().
  </action>
  <verify>
Read each modified SKILL.md and verify:
1. No broken markdown (the bash code blocks are properly fenced)
2. The create_phase_dir() call uses the correct variable ($PHASE_NUM in discuss/plan, special handling for quick-plan)
3. The phase-utils.sh source line is present before the call (already exists from Phase 2 normalize_phase usage)
4. No duplicate source/normalize calls introduced

Verify quick-plan still has a functional directory creation mechanism (either create_phase_dir or documented inline code).
  </verify>
  <done>discuss-phase and plan-phase SKILL.md files use create_phase_dir() instead of inline pipelines. quick-plan either uses create_phase_dir() or retains documented inline creation with an explanatory comment.</done>
</task>

<task type="auto">
  <name>Task 2: Add STATE.md decision archival to execute-phase compaction</name>
  <files>plugins/claude-super-team/skills/execute-phase/SKILL.md</files>
  <action>
In execute-phase/SKILL.md, Phase 8 (Update State) already compacts ROADMAP.md and updates STATE.md. Add a new compaction step that moves completed-phase decisions to a Decision Archive section.

Add the following logic to Phase 8 of execute-phase/SKILL.md, after the existing STATE.md updates and before the ROADMAP.md updates:

**STATE.md Decision Compaction:**

After updating STATE.md with the current phase's execution status:

1. Read STATE.md's `### Decisions` section
2. Identify decision entries that reference the just-completed phase (look for entries starting with `- Phase {N}:` or `- Phase {PHASE_NUM}:` or entries that mention the phase number in their text)
3. Move those entries from `### Decisions` to `### Decision Archive` section
4. If `### Decision Archive` does not exist yet, create it after the `### Blockers/Concerns` section (or at the end of `## Accumulated Context`)
5. Keep the archive delimiter as `### Decision Archive` -- this is what gather scripts will use to stop reading

**Important:** Also ensure that the gather scripts (via gather-common.sh's emit_state_section) respect the archive delimiter. Add to the instructions: "The `emit_state_section` function in gather-common.sh should emit STATE.md content only up to the `### Decision Archive` delimiter (exclusive). If no delimiter exists, emit the entire file. This keeps archived decisions out of context when skills load STATE.md."

Add this instruction to Phase 8 of execute-phase/SKILL.md right after the existing "Update `.planning/STATE.md` with:" bullet list. Frame it as an additional compaction step similar to the existing ROADMAP.md compaction.

Also: In plan 09-01 (gather-common.sh), the emit_state_section function must truncate output at `### Decision Archive`. If plan 01 didn't include this, add a note here that the executor should update gather-common.sh's emit_state_section to stop reading at `### Decision Archive` -- use `awk` or `sed` to truncate. Only do this if emit_state_section doesn't already handle it.

Replace the execute-phase Phase 3 inline `PHASE_DIR=$(ls -d ...)` with `PHASE_DIR=$(create_phase_dir "$PHASE_NUM")` -- execute-phase already sources phase-utils.sh in Phase 2 for normalize_phase. This is the 4th skill that needs the migration.
  </action>
  <verify>
Read execute-phase/SKILL.md and verify:
1. Phase 8 includes the decision archival step
2. The archival logic specifies `### Decision Archive` as the delimiter
3. The instructions for truncating gather output at the delimiter are clear
4. Phase 3 uses create_phase_dir() instead of inline ls/mkdir
5. No other SKILL.md sections are accidentally modified
  </verify>
  <done>execute-phase/SKILL.md Phase 8 includes STATE.md decision archival (moving completed-phase decisions below ### Decision Archive). Phase 3 uses create_phase_dir(). gather-common.sh emit_state_section truncates at the archive delimiter.</done>
</task>

</tasks>

<verification>
1. discuss-phase and plan-phase SKILL.md files reference create_phase_dir() for directory creation
2. execute-phase SKILL.md Phase 3 references create_phase_dir() and Phase 8 includes decision archival
3. quick-plan SKILL.md either uses create_phase_dir() or has documented rationale for inline creation
4. The Decision Archive delimiter (### Decision Archive) is consistently referenced
5. gather-common.sh emit_state_section truncates output at ### Decision Archive
</verification>

<success_criteria>
4 SKILL.md files no longer contain inline phase directory creation pipelines (replaced by create_phase_dir() or documented exception). execute-phase compaction archives completed-phase decisions in STATE.md below ### Decision Archive. Gather scripts emit STATE.md content only above the archive delimiter.
</success_criteria>

<output>
After completion, create `.planning/phases/09-script-consolidation-state-compaction/09-03-SUMMARY.md`
</output>
