---
phase: 07.1-build-skill-efficiency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-super-team/skills/execute-phase/SKILL.md
  - plugins/claude-super-team/skills/execute-phase/gather-data.sh
  - plugins/claude-super-team/skills/build/references/pipeline-guide.md
  - plugins/claude-super-team/skills/build/SKILL.md
  - plugins/claude-super-team/skills/plan-phase/references/planner-guide.md
autonomous: true
quick_phase: true

must_haves:
  truths:
    - "Execute-phase detects teams availability from gather script PREFERENCES output, not from environment variable"
    - "/build's pipeline depth heuristic considers project complexity class and tech stack coverage beyond keyword matching"
    - "A verification preference in STATE.md (always|on-failure|disabled) controls whether deep verification runs after phase execution"
    - "Planner guide wave assignment rules favor same-wave placement for plans with non-overlapping files or read-only shared deps"
  artifacts:
    - path: "plugins/claude-super-team/skills/execute-phase/SKILL.md"
      provides: "Teams detection from PREFERENCES section, verification preference reading and conditional verifier spawn"
    - path: "plugins/claude-super-team/skills/execute-phase/gather-data.sh"
      provides: "Grep line for verification preference from STATE.md"
    - path: "plugins/claude-super-team/skills/build/references/pipeline-guide.md"
      provides: "Enhanced adaptive pipeline depth heuristic with complexity class, tech stack coverage, cumulative knowledge discount"
    - path: "plugins/claude-super-team/skills/build/SKILL.md"
      provides: "Verification preference resolution in Step 3 and passing to execute-phase"
    - path: "plugins/claude-super-team/skills/plan-phase/references/planner-guide.md"
      provides: "Strengthened wave batching rules favoring parallelism"
  key_links:
    - from: "plugins/claude-super-team/skills/execute-phase/gather-data.sh"
      to: "plugins/claude-super-team/skills/execute-phase/SKILL.md"
      via: "gather-data.sh outputs verification preference; SKILL.md Phase 3.7 reads it"
    - from: "plugins/claude-super-team/skills/build/SKILL.md"
      to: "plugins/claude-super-team/skills/execute-phase/SKILL.md"
      via: "/build resolves verification preference to STATE.md; execute-phase reads it from PREFERENCES"
---

<objective>
Fix teams detection bug, add a verification preference to skip deep verification when build+tests pass clean, enhance /build's adaptive pipeline depth heuristic with project complexity class and tech stack coverage, and strengthen planner wave batching rules to favor parallelism.

Purpose: These are targeted efficiency improvements for the /build pipeline -- fixing a bug where teams detection reads an inaccessible env var, reducing unnecessary verification overhead, making pipeline depth smarter, and enabling more parallel plan execution.
Output: Modified SKILL.md files for execute-phase and build, updated gather-data.sh, enhanced pipeline-guide.md, and improved planner-guide.md wave assignment section.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix teams detection bug and add verification preference to execute-phase</name>
  <files>
    plugins/claude-super-team/skills/execute-phase/SKILL.md
    plugins/claude-super-team/skills/execute-phase/gather-data.sh
  </files>
  <action>
**gather-data.sh** -- Add a grep line for the verification preference after the existing simplifier grep (line 20). Insert between line 20 and line 21:
```
grep -E '^verification:' .planning/STATE.md 2>/dev/null || echo "verification: unset"
```
This follows the exact same pattern as the execution-model and simplifier lines above it.

**SKILL.md Phase 2 (lines 91-96)** -- Fix the teams detection bug. Replace line 95:
```
- Else if `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` in environment: `EXEC_MODE=team`
```
with:
```
- Else if `teams-available: true` in the pre-loaded PREFERENCES section: `EXEC_MODE=team`
```

**SKILL.md Phase 2.5 (lines 122-124)** -- Update the log message for the environment variable case to match the new detection method. Replace:
```
**If `EXEC_MODE=team` triggered by environment variable:**
```Using teams mode (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1).```
```
with:
```
**If `EXEC_MODE=team` triggered by PREFERENCES:**
```Using teams mode (teams-available: true in preferences).```
```

Also update the task-mode message on lines 127-129. Replace:
```
**If `EXEC_MODE=task`:**
```Using task mode -- teams not enabled. Pass --team or set CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 to use teams.```
```
with:
```
**If `EXEC_MODE=task`:**
```Using task mode -- teams not enabled. Pass --team to use teams.```
```

**SKILL.md -- Add Phase 3.7: Resolve Verification Preference** after Phase 3.6 (after line 206). Insert a new section:

```markdown
### Phase 3.7: Resolve Verification Preference

Use the pre-loaded **PREFERENCES** section from the gather script. Check the `verification` value.

**If preference is set:** Use its value (`always`, `on-failure`, or `disabled`) as `$VERIFICATION_PREF`.

**If preference is NOT set (missing or "unset"):** Default to `on-failure` silently (no user prompt).

Log the resolved value:

```
Verification: {always|on-failure|disabled}
```

**Effect on Phase 6 (Verify Phase Goal):**

- If `$VERIFICATION_PREF` is `always`: Run the verifier (current behavior).
- If `$VERIFICATION_PREF` is `on-failure`: Skip the verifier ONLY when ALL of these are true: (1) all plans completed without errors in their SUMMARY.md files, (2) no spot-check failures occurred during wave execution, (3) `--skip-verify` was NOT explicitly set (respect explicit flags). If any plan had errors or spot-checks failed, run the verifier.
- If `$VERIFICATION_PREF` is `disabled`: Skip the verifier entirely (same as `--skip-verify`).
```

**SKILL.md Phase 6 (line 280)** -- Update the skip condition. Replace:
```
Skip if `--skip-verify` flag was set.
```
with:
```
Skip if `--skip-verify` flag was set OR if `$VERIFICATION_PREF` is `disabled`.

If `$VERIFICATION_PREF` is `on-failure`: Skip the verifier when all plans completed cleanly (no errors in SUMMARY.md files, no spot-check failures). Run the verifier if any plan reported errors or spot-checks failed.

If `$VERIFICATION_PREF` is `always`: Always run the verifier (current default behavior).
```
  </action>
  <verify>
Read gather-data.sh and confirm it has 4 grep/echo lines in PREFERENCES section (execution-model, simplifier, verification, teams-available).
Read SKILL.md Phase 2 and confirm no reference to `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var remains.
Read SKILL.md and confirm Phase 3.7 exists between 3.6 and 4.
Read SKILL.md Phase 6 and confirm verification preference logic is present.
  </verify>
  <done>
Execute-phase reads teams-available from PREFERENCES (not env var). Verification preference is read from PREFERENCES with on-failure default. Phase 6 conditionally spawns verifier based on the preference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance /build adaptive pipeline depth and verification preference resolution</name>
  <files>
    plugins/claude-super-team/skills/build/references/pipeline-guide.md
    plugins/claude-super-team/skills/build/SKILL.md
  </files>
  <action>
**pipeline-guide.md Section 2** -- Add three new subsections after the existing "### Default Behavior" subsection (after line 61) and before "### Constraints" (line 64):

1. **Tech Stack Coverage** subsection:
```markdown
### Tech Stack Coverage

If `build-preferences.md` specifies a full tech stack (framework, database, auth provider, etc.) AND the current phase's goal and success criteria use only technologies within that stack, bias toward SIMPLE. The rationale: when the user has already made all tech decisions, /discuss-phase adds no value and /research-phase is redundant for well-known frameworks.

Conversely, if the phase introduces a technology NOT covered by build-preferences (e.g., a payment provider when preferences only specify frontend + backend), run FULL.
```

2. **Project Complexity Class** subsection:
```markdown
### Project Complexity Class

After /create-roadmap completes (Step 8), classify the project as `standard` or `complex` based on the roadmap:

- **complex** if ANY: total phase count >= 8, any phase has >= 5 success criteria, >= 3 phases mention external service integration, project involves real-time features or distributed systems
- **standard** otherwise

Log the classification in BUILD-STATE.md under Session as `Complexity class: {standard|complex}`.

Effect on pipeline depth: For `standard` projects, simplicity signals are weighted more heavily -- a phase matching 2+ simplicity signals skips discuss/research even if it also matches 1 complexity signal (keyword match alone is not enough to trigger FULL). For `complex` projects, the existing default-to-FULL behavior applies unchanged.
```

3. **Cumulative Knowledge Discount** subsection:
```markdown
### Cumulative Knowledge Discount

After phases 1-2 have been executed, later phases that follow the same patterns established in earlier phases lean toward SIMPLE even if they mention domains that sound "new" but are structurally similar (e.g., "new API endpoints" in a phase that follows the same route/controller/model pattern as phase 2).

Specifically: if a phase's success criteria reference file patterns already created in earlier phases (same directory structure, same file naming, same tech stack), and prior CONTEXT.md/RESEARCH.md already cover the relevant domain, treat it as pattern-following and bias SIMPLE.

This discount does NOT apply when the phase introduces genuinely new infrastructure (new database, new external service, new deployment target).
```

**build SKILL.md Step 3 (lines 183-198)** -- Add verification preference resolution. After the existing preference variables (line 195), add a new bullet:
```
- `$PREF_VERIFICATION` -- verification preference (always, on-failure, or disabled). Default: on-failure.
```

**build SKILL.md Step 8 (around line 403)** -- After "Extract phase numbers, names, goals, and success criteria from the roadmap." (line 403), add project complexity classification:
```
Classify the project complexity. Read the phase list from ROADMAP.md:
- Count total phases
- For each phase, count success criteria
- Count phases mentioning external service integration
- Check for real-time features or distributed systems

If ANY: total phases >= 8, any phase has >= 5 success criteria, >= 3 phases mention external integrations, or project involves real-time/distributed: `COMPLEXITY_CLASS = complex`. Otherwise: `COMPLEXITY_CLASS = standard`.

Log in BUILD-STATE.md under Session: `Complexity class: {standard|complex}`.
```

**build SKILL.md Step 9g (around line 607)** -- After the `Skill('execute-phase', '{N}')` invocation line, the skill needs to pass the verification preference. Before the AskUserQuestion handling bullets, add a note:
```
The verification preference (`$PREF_VERIFICATION`) is already persisted in STATE.md from Step 3 preference resolution. Execute-phase reads it from its gather script PREFERENCES section automatically.
```

**build SKILL.md -- Persist verification preference.** In Step 4 (around line 226), after writing BUILD-STATE.md, add a step to persist $PREF_VERIFICATION to STATE.md's Preferences section (same pattern as execution-model):
```
If `$PREF_VERIFICATION` was resolved from preferences, persist it to STATE.md under `## Preferences`:
```markdown
verification: {$PREF_VERIFICATION}
```
Use Edit tool to update STATE.md. If the preference line already exists, update it. If not, add it below the existing preferences.
```
  </action>
  <verify>
Read pipeline-guide.md and confirm three new subsections exist between "Default Behavior" and "Constraints": Tech Stack Coverage, Project Complexity Class, Cumulative Knowledge Discount.
Read build SKILL.md Step 3 and confirm $PREF_VERIFICATION is listed with default on-failure.
Read build SKILL.md Step 8 and confirm complexity classification logic exists.
Grep for "verification" in build SKILL.md to confirm preference persistence.
  </verify>
  <done>
Pipeline depth heuristic has three new dimensions (tech stack coverage, complexity class, cumulative knowledge discount). /build resolves and persists verification preference to STATE.md. Complexity class is computed after /create-roadmap and logged in BUILD-STATE.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strengthen planner wave batching rules</name>
  <files>plugins/claude-super-team/skills/plan-phase/references/planner-guide.md</files>
  <action>
**planner-guide.md "Dependency Graph" section (lines 109-136)** -- Replace the wave assignment algorithm and surrounding guidance with strengthened batching rules. Keep the existing `for each plan` algorithm block (lines 115-118) but replace everything from line 114 ("**Wave assignment algorithm:**") through line 136 ("Overlap = must be sequential.") with:

```markdown
**Wave assignment rules:**

1. Plans touching **non-overlapping file sets** -> assign to the **same wave**
2. Plans sharing only **read-only dependencies** (both read a file but neither writes it) -> assign to the **same wave**
3. Sequential waves ONLY when plan A's **output** is plan B's **input** (plan B's `depends_on` references plan A, AND plan B's tasks use files/exports created by plan A)
4. When in doubt, **same wave** -- execute-phase handles coordination and the cost of unnecessary sequencing (idle agents, wasted context) exceeds the cost of a minor merge conflict

```
for each plan:
  if depends_on is empty: wave = 1
  else if depends_on plans only READ files this plan also reads (no write conflicts): wave = same as dependency
  else: wave = max(wave of each dependency) + 1
```

**Vertical slices strongly preferred.** Each plan should be a complete vertical slice (model + API + UI for one feature). Two vertical slices touching different features go in the same wave even if they share a common utility file -- the executor handles the merge.

```
Plan 01: User feature (model + API + UI)    -- Wave 1
Plan 02: Product feature (model + API + UI) -- Wave 1
```
Both run in parallel. Even if both import from `src/lib/db.ts`, they only READ it -- no write conflict.

**Horizontal layers -- actively avoid:**
```
Plan 01: All models    -- Wave 1
Plan 02: All APIs      -- Wave 2 (needs 01)
Plan 03: All UI        -- Wave 3 (needs 02)
```
This creates unnecessary sequential bottlenecks. Restructure as vertical slices.

**File ownership clarity:** No write-overlap in `files_modified` between same-wave plans. Two plans that both CREATE or MODIFY the same file must be in different waves. Two plans that both READ the same file (imports, shared config) can be in the same wave.
```

This replaces lines 114-136 of the original file. The content before line 114 ("For each task, record:" etc.) and after line 136 (the "## Scope Estimation" section) remains unchanged.
  </action>
  <verify>
Read planner-guide.md Dependency Graph section and confirm:
- 4 numbered wave assignment rules exist (non-overlapping files, read-only deps, sequential only for output->input, default same wave)
- Updated algorithm block includes read-conflict check
- Vertical slices section includes the shared utility file example
- "File ownership clarity" distinguishes write-overlap from read-overlap
  </verify>
  <done>
Wave assignment rules explicitly favor same-wave placement for non-overlapping and read-only-shared plans. The default is same-wave (not sequential). Vertical slices are strongly preferred with concrete examples showing shared reads are safe.
  </done>
</task>

</tasks>

<verification>
All four success criteria are met:
1. Execute-phase reads teams-available from PREFERENCES (Task 1 -- gather-data.sh already outputs it, SKILL.md now reads it)
2. Pipeline depth heuristic has tech stack coverage, complexity class, and cumulative knowledge discount (Task 2 -- pipeline-guide.md)
3. Verification preference in STATE.md controls verifier behavior (Task 1 + Task 2 -- gather-data.sh greps it, execute-phase reads it, /build resolves and persists it)
4. Planner wave batching is more aggressive (Task 3 -- planner-guide.md)
</verification>

<success_criteria>
- Execute-phase SKILL.md contains no reference to CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS environment variable
- Execute-phase gather-data.sh outputs a verification preference line in PREFERENCES section
- Execute-phase SKILL.md has Phase 3.7 for verification preference resolution and Phase 6 uses it
- Pipeline-guide.md has Tech Stack Coverage, Project Complexity Class, and Cumulative Knowledge Discount subsections
- Build SKILL.md resolves and persists $PREF_VERIFICATION to STATE.md
- Planner-guide.md wave assignment rules default to same-wave and list 4 explicit rules
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-build-skill-efficiency/07.1-01-SUMMARY.md`
</output>
