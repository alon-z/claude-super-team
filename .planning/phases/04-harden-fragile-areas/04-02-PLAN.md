---
phase: 04-harden-fragile-areas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-super-team/skills/build/SKILL.md
  - plugins/claude-super-team/skills/build/references/sprint-execution-guide.md
  - plugins/claude-super-team/skills/build/references/finalization-guide.md
autonomous: true

must_haves:
  truths:
    - "build/SKILL.md is under 500 lines"
    - "The build skill's behavior is identical before and after decomposition -- pure structural refactor"
    - "Extracted reference documents are loaded via Read() inline at the point where the SKILL.md previously had the content"
  artifacts:
    - "plugins/claude-super-team/skills/build/references/sprint-execution-guide.md containing Step 8-E and Step 9"
    - "plugins/claude-super-team/skills/build/references/finalization-guide.md containing Steps 10-13 and Success Criteria"
    - "plugins/claude-super-team/skills/build/SKILL.md reduced to under 500 lines"
  key_links:
    - "SKILL.md reads sprint-execution-guide.md at the point where Steps 8-E/9 content was, with a Read() call and instruction to follow it"
    - "SKILL.md reads finalization-guide.md at the point where Steps 10-13 content was, with a Read() call and instruction to follow it"
    - "Reference documents preserve the exact same heading hierarchy (###, ####) and step numbering so cross-references from other parts of SKILL.md remain valid"
---

<objective>
Decompose build/SKILL.md (1084 lines) into the main SKILL.md plus two reference documents, targeting under 500 lines for the SKILL.md.

Purpose: Large skill files degrade maintainability. The build skill is 2x the 500-line threshold. Extracting self-contained procedural blocks into references preserves behavior while making each file manageable.
Output: Two new reference documents and a reduced SKILL.md.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-harden-fragile-areas/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Steps 8-E and 9 into sprint-execution-guide.md</name>
  <files>
plugins/claude-super-team/skills/build/references/sprint-execution-guide.md
plugins/claude-super-team/skills/build/SKILL.md
  </files>
  <action>
Create `plugins/claude-super-team/skills/build/references/sprint-execution-guide.md` containing Step 8-E and the entire Step 9 from SKILL.md.

**What to extract:** Two contiguous sections:
1. Step 8-E: "### Step 8-E: Invoke /create-roadmap (Extend Mode)" (line 455) through the end of that section (line 506, just before the `---` separator at line 507).
2. Step 9: "### Step 9: Sprint Execution Loop" (line 509) through the end of "#### 9i. Complete Sprint" (line 885, just before the `---` separator at line 886).

**Reference document format:**
- Title: `# Sprint Execution Guide`
- Description: `Steps 8-E and 9 of the /build pipeline. Covers extend-mode roadmap creation and the full sprint execution loop (discuss/research, parallel planning, sequential execution, per-phase validation/feedback/merge, sprint boundary validation).`
- Then the full extracted content, preserving ALL headings, code blocks, instructions, and formatting exactly as they appear in SKILL.md. Keep the `### Step` headings as-is (the executor needs step numbers for cross-references from Step 1 resume logic).

**In SKILL.md**, replace the extracted region (lines 455-886, including the `---` separators around them) with a compact stub:

```markdown
### Step 8-E: Invoke /create-roadmap (Extend Mode)

### Step 9: Sprint Execution Loop

Read the sprint execution guide for Step 8-E (extend-mode roadmap creation) and the complete sprint execution loop:

\`\`\`
Read('${CLAUDE_SKILL_DIR}/references/sprint-execution-guide.md')
\`\`\`

(Resolved path: `${CLAUDE_PLUGIN_ROOT}/skills/build/references/sprint-execution-guide.md`)

**Step 8-E** covers extend-mode /create-roadmap invocation, sprint map rebuild, and phase progress initialization for extend builds. After completion, continue to Step 9.

**Step 9** is the core execution loop. Follow it for each sprint in SPRINT_MAP. It covers:
- 9-pre: Skip completed sprints
- 9a-9c: Sprint setup, adaptive pipeline depth, discuss + research
- 9d: Plan all sprint phases (parallel)
- 9e: Git branching for sprint phases
- 9f: Execute all sprint phases (sequential, separate branches)
- 9g: Per-phase validation, feedback, and merge (9g-i through 9g-v)
- 9h: Sprint boundary validation
- 9i: Complete sprint

After the sprint execution loop completes (all sprints processed), continue to Steps 10-13 below.
```

This stub is approximately 24 lines, replacing the extracted ~431 lines (lines 455-886).

**Critical:** The reference document must be a verbatim extraction. Do not rewrite, summarize, or restructure any content. The executor reads this reference and follows it exactly as if it were inline in SKILL.md.
  </action>
  <verify>
1. `test -f plugins/claude-super-team/skills/build/references/sprint-execution-guide.md` succeeds
2. The reference contains Step 8-E: `grep -q "Step 8-E" plugins/claude-super-team/skills/build/references/sprint-execution-guide.md`
3. The reference contains all Step 9 subsections 9-pre through 9i: `grep -c "^####" plugins/claude-super-team/skills/build/references/sprint-execution-guide.md` should return at least 10
4. SKILL.md contains `Read('${CLAUDE_SKILL_DIR}/references/sprint-execution-guide.md')` where the content was
5. SKILL.md no longer contains `#### 9a. Sprint Setup` or `#### 9g-iii. Squash-Merge to Main` (moved to reference)
6. SKILL.md no longer contains `EXTEND_CANDIDATE` or `Skill('create-roadmap', 'add $ARGUMENTS')` (moved to reference with Step 8-E)
  </verify>
  <done>Sprint execution guide reference exists with Step 8-E and complete Step 9 content. SKILL.md has a compact stub pointing to it. No behavioral change.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Steps 10-13 and Success Criteria into finalization-guide.md</name>
  <files>
plugins/claude-super-team/skills/build/references/finalization-guide.md
plugins/claude-super-team/skills/build/SKILL.md
  </files>
  <action>
Create `plugins/claude-super-team/skills/build/references/finalization-guide.md` containing Steps 10 through 13 and the Success Criteria section from SKILL.md.

**What to extract:** Everything from `### Step 10: Final Validation` (line 888) through the end of `## Success Criteria` (line 1084, end of file). This includes Steps 10, 11, 12, 13, and the Success Criteria checklist.

**Reference document format:**
- Title: `# Finalization Guide`
- Description: `Steps 10-13 of the /build pipeline. Final validation, auto-fix loop, BUILD-REPORT.md generation, completion summary, and success criteria.`
- Then the full extracted content, preserving ALL headings, code blocks, instructions, and formatting exactly as they appear in SKILL.md. Keep the `### Step N:` headings.

**In SKILL.md**, replace the extracted content (lines 886 through 1084 -- from the `---` separator after Step 9 to end of file) with a compact stub:

```markdown
---

### Steps 10-13: Finalization

Read the finalization guide for the complete end-of-pipeline procedure:

\`\`\`
Read('${CLAUDE_SKILL_DIR}/references/finalization-guide.md')
\`\`\`

(Resolved path: `${CLAUDE_PLUGIN_ROOT}/skills/build/references/finalization-guide.md`)

Follow the finalization procedure from the guide. It covers:
- Step 10: Final validation (always run build + tests after all phases)
- Step 11: Auto-fix loop (max 3 attempts on failure)
- Step 12: Generate BUILD-REPORT.md from BUILD-STATE.md data
- Step 13: Present completion summary to user
- Success criteria checklist

**END OF PROCESS.**
```

This stub is approximately 18 lines, replacing ~197 lines.

**After both tasks complete**, verify the final SKILL.md line count. The math:
- Original: 1084 lines
- Removed: ~431 lines (Step 8-E + Step 9) + ~197 lines (Steps 10-13 + Success Criteria) = ~628 lines removed
- Added: ~24 lines (Task 1 stub) + ~18 lines (Task 2 stub) = ~42 lines added
- Expected result: 1084 - 628 + 42 = ~498 lines (under 500)

**Critical:** Verbatim extraction. Do not rewrite or restructure. The Success Criteria section moves to the reference because it describes finalization-stage acceptance criteria and would be orphaned from its context in the stub-only SKILL.md.
  </action>
  <verify>
1. `test -f plugins/claude-super-team/skills/build/references/finalization-guide.md` succeeds
2. The reference contains Steps 10-13: `grep -c "^### Step" plugins/claude-super-team/skills/build/references/finalization-guide.md` should return 4
3. `grep -q "Success Criteria" plugins/claude-super-team/skills/build/references/finalization-guide.md` succeeds
4. SKILL.md contains `Read('${CLAUDE_SKILL_DIR}/references/finalization-guide.md')` where Steps 10-13 were
5. **SKILL.md line count is under 500:** `wc -l plugins/claude-super-team/skills/build/SKILL.md` shows < 500
6. SKILL.md no longer contains `### Step 11: Auto-Fix Loop` or `### Step 13: Present Completion Summary` (moved to reference)
7. The existing references (autonomous-decision-guide.md, pipeline-guide.md) are untouched
8. The SKILL.md frontmatter is unchanged (first 17 lines identical to original)
  </verify>
  <done>Finalization guide reference exists with Steps 10-13 + Success Criteria. SKILL.md has a compact stub pointing to it. Total SKILL.md line count is under 500. No behavioral change.</done>
</task>

</tasks>

<verification>
1. `wc -l plugins/claude-super-team/skills/build/SKILL.md` < 500 lines
2. Both reference documents exist in `plugins/claude-super-team/skills/build/references/`
3. SKILL.md contains Read() calls for both references at the correct insertion points
4. No content was lost: every heading from Steps 8-E through Success Criteria exists in one of the two reference documents
5. SKILL.md still contains Steps 0-8 in full (these were not extracted)
6. The frontmatter of SKILL.md is unchanged
7. The existing references (autonomous-decision-guide.md, pipeline-guide.md) are untouched
8. Total reference file count in build/references/: 4 (autonomous-decision-guide.md, pipeline-guide.md, sprint-execution-guide.md, finalization-guide.md)
</verification>

<success_criteria>
build/SKILL.md is under 500 lines. Two new reference documents contain the extracted content verbatim. The SKILL.md stubs point to the references with Read() calls and include a summary of what each reference covers. The build skill's behavior is identical -- this is a pure structural refactor with no logic changes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-harden-fragile-areas/04-02-SUMMARY.md`
</output>
