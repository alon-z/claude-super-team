---
phase: 07-metrics-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-super-team/.claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - plugins/claude-super-team/skills/cst-help/SKILL.md
  - plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
  - plugins/claude-super-team/skills/cst-help/references/troubleshooting.md

autonomous: true

must_haves:
  truths:
    - "User can discover /metrics through /cst-help skill reference and workflow guide"
    - "/metrics appears in plugin.json keywords and marketplace.json description"
    - "User can troubleshoot common /metrics issues via /cst-help troubleshooting"
  artifacts:
    - plugins/claude-super-team/.claude-plugin/plugin.json
    - .claude-plugin/marketplace.json
    - plugins/claude-super-team/skills/cst-help/SKILL.md
    - plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
    - plugins/claude-super-team/skills/cst-help/references/troubleshooting.md
  key_links:
    - "plugin.json keywords array includes 'metrics' or 'telemetry' so the skill is discoverable"
    - "marketplace.json description for claude-super-team mentions metrics/telemetry capability"
    - "cst-help SKILL.md Skill Reference section lists /metrics with description and usage"
    - "workflow-guide.md includes /metrics in the pipeline overview and Analysis section"
    - "troubleshooting.md has 'When to Use /metrics' section and common issue entries"
---

<objective>
Register /metrics in plugin manifests and update /cst-help documentation to include the new skill.

Purpose: Without registration and documentation, users cannot discover or get help with /metrics. This plan completes the integration of the skill into the plugin ecosystem.

Output: Updated plugin.json, marketplace.json, and three cst-help files (SKILL.md, workflow-guide.md, troubleshooting.md) with /metrics entries.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register /metrics in plugin and marketplace manifests</name>
  <files>
plugins/claude-super-team/.claude-plugin/plugin.json
.claude-plugin/marketplace.json
  </files>
  <action>
**plugin.json** (`plugins/claude-super-team/.claude-plugin/plugin.json`):
- Add `"metrics"` and `"telemetry"` to the `keywords` array
- Update the `description` to mention telemetry metrics reporting (append to existing description, keep it concise -- add something like ", telemetry metrics reporting" near the end)
- Bump `version` from `1.0.49` to `1.0.50`

**marketplace.json** (`.claude-plugin/marketplace.json`):
- In the `claude-super-team` plugin entry, add `"metrics"` and `"telemetry"` to `keywords`
- Update the `description` to mention telemetry metrics (same addition as plugin.json)
- Bump `version` in the claude-super-team entry from `1.0.49` to `1.0.50`
- Bump the top-level marketplace `version` from `1.0.49` to `1.0.50`
  </action>
  <verify>
Read both files and verify:
- plugin.json keywords contains "metrics" and "telemetry"
- plugin.json version is "1.0.50"
- marketplace.json claude-super-team entry has matching keywords, description, and version
- marketplace.json top-level version is "1.0.50"
- JSON is valid (no trailing commas, proper formatting)
  </verify>
  <done>Both manifests register /metrics with updated keywords, descriptions, and version 1.0.50.</done>
</task>

<task type="auto">
  <name>Task 2: Update /cst-help with /metrics documentation</name>
  <files>
plugins/claude-super-team/skills/cst-help/SKILL.md
plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
plugins/claude-super-team/skills/cst-help/references/troubleshooting.md
  </files>
  <action>
**cst-help SKILL.md** -- Update the "Help Response: Skill Reference" section:

Add a new "## Analysis" subsection (or append to the existing Analysis section that contains /drift) with:
```
/metrics
  Analyze telemetry data for resource usage and threshold violations
  -> Reads .planning/.telemetry/ session files
  -> Reports per-session tool calls, agent spawns, duration, failure rates
  -> Flags violations against configurable thresholds (.planning/.telemetry/config.json)
  -> Displays report in conversation (no file output)
```

Also update the ad-hoc/general workflow answer section to mention /metrics as an analysis tool alongside /drift.

**workflow-guide.md** -- Make these updates:

1. In the "Analysis" subsection of "The Sequential Pipeline", add:
```
/metrics           -> Analyze telemetry data for resource usage and threshold violations
                      Reads .planning/.telemetry/ session files (captured by hook-based telemetry)
                      Reports per-session: tool calls, agents, failures, duration
                      Compares against configurable thresholds in .planning/.telemetry/config.json
```

2. In the "File Structure Reference" section, under `.planning/`, add:
```
├── .telemetry/                          # Telemetry data (captured by hooks)
│   ├── config.json                      # Threshold configuration (optional)
│   └── session-{id}.jsonl               # Per-session event logs
```

3. In "When to Use Each Skill" / "Workflow Patterns", add a subsection:

```
### Use /metrics when:
- Want to see resource usage across sessions (tool calls, agent spawns, durations)
- Checking if any session exceeded configured thresholds
- Reviewing efficiency after completing several phases
- Diagnosing slow or resource-heavy skill invocations
```

**troubleshooting.md** -- Add these entries:

1. Under a new "### Telemetry and Metrics" heading (place after "Drift Detection"):

```
#### "No telemetry data found"

**Symptom:** `/metrics` reports no data

**Cause:** No `.planning/.telemetry/` directory or no session files

**Solution:**
- Telemetry is captured automatically by hook-based instrumentation
- Run any skill (e.g., `/progress`) to generate telemetry data
- Check `.planning/.telemetry/` for session-*.jsonl files

#### "Thresholds not taking effect"

**Symptom:** Custom thresholds in config.json ignored

**Cause:** config.json format incorrect or file not found

**Solution:**
- File must be at `.planning/.telemetry/config.json`
- Must be valid JSON with numeric values
- Example: `{"max_tool_calls_per_session": 300, "max_session_duration_minutes": 45}`
- Default thresholds apply when config.json is missing

#### "Duration shows 0 for all sessions"

**Symptom:** All sessions report 0 duration

**Cause:** Timestamp parsing failed (platform-specific date command differences)

**Solution:**
- This is cosmetic -- other metrics (tool counts, agents, failures) are still accurate
- Ensure sessions have both skill_start and skill_end events
```

2. In "When to Use Each Skill", add:
```
### Use `/metrics` when:
- Want to review telemetry data across sessions
- Checking for threshold violations after running several skills
- Diagnosing which sessions used the most resources
- Before planning new phases, to understand typical resource usage
```
  </action>
  <verify>
Read all three updated files. Verify:
- cst-help SKILL.md: /metrics appears in the Skill Reference output section under Analysis
- workflow-guide.md: /metrics in pipeline overview, .telemetry/ in file structure, /metrics in "When to Use" section
- troubleshooting.md: Three /metrics troubleshooting entries exist, "When to Use /metrics" section exists
- No existing content was accidentally removed or broken
  </verify>
  <done>All three cst-help files document /metrics: SKILL.md has it in Skill Reference, workflow-guide.md has it in pipeline/structure/patterns, troubleshooting.md has diagnostic entries and usage guidance.</done>
</task>

</tasks>

<verification>
1. `grep -l "metrics" plugins/claude-super-team/.claude-plugin/plugin.json .claude-plugin/marketplace.json` returns both files
2. `grep "metrics" plugins/claude-super-team/skills/cst-help/SKILL.md` shows /metrics in skill reference
3. `grep "metrics" plugins/claude-super-team/skills/cst-help/references/workflow-guide.md` shows pipeline entry and usage section
4. `grep "metrics" plugins/claude-super-team/skills/cst-help/references/troubleshooting.md` shows troubleshooting entries
5. JSON files parse without errors
</verification>

<success_criteria>
/metrics is registered in both plugin.json and marketplace.json with version 1.0.50. All three cst-help reference files document /metrics for discovery, workflow integration, and troubleshooting. Users can find /metrics through the help system and understand when and how to use it.
</success_criteria>

<output>
After completion, create `.planning/phases/07-metrics-skill/07-02-SUMMARY.md`
</output>
