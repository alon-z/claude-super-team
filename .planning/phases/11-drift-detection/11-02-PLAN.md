---
phase: 11-drift-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/claude-super-team/skills/cst-help/SKILL.md
  - plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
  - plugins/claude-super-team/skills/cst-help/references/troubleshooting.md
autonomous: true

must_haves:
  truths:
    - "/cst-help skill reference section lists /drift with description, output, and usage example"
    - "workflow-guide.md includes /drift in the pipeline overview and explains when to use it"
    - "troubleshooting.md includes drift-related troubleshooting entries"
  artifacts:
    - "plugins/claude-super-team/skills/cst-help/SKILL.md (updated)"
    - "plugins/claude-super-team/skills/cst-help/references/workflow-guide.md (updated)"
    - "plugins/claude-super-team/skills/cst-help/references/troubleshooting.md (updated)"
  key_links:
    - "SKILL.md skill reference output matches what workflow-guide.md describes for /drift"
    - "troubleshooting.md When to Use Each Skill section includes /drift"
---

<objective>
Update the /cst-help skill and its reference files to include the new /drift skill, so users can discover and get help with drift detection.

Purpose: CLAUDE.md mandates keeping /cst-help in sync when adding skills. This plan ensures /drift is discoverable via the help system.
Output: Updated cst-help SKILL.md, workflow-guide.md, and troubleshooting.md with /drift entries.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update /cst-help SKILL.md skill reference</name>
  <files>plugins/claude-super-team/skills/cst-help/SKILL.md</files>
  <action>
In the "Help Response: Skill Reference" section of `plugins/claude-super-team/skills/cst-help/SKILL.md`, add a `/drift` entry. Place it in a new "## Analysis" subsection after the "## Ad-Hoc Extensions" block and before "## Full Automation".

The entry should follow the exact format used by other skills in that section:

```
## Analysis

/drift [N | --all]
  Compare codebase against planning artifacts to detect divergence
  Reads SUMMARY.md, CONTEXT.md, PLAN.md claims and verifies via Explore agents
  -> Creates .planning/DRIFT-REPORT.md with categorized findings (confirmed/potential/aligned)
```

Also update the general workflow question answer in the "Help Response: General Workflow Question" section. In the "Ad-hoc skills" paragraph, add `/drift` alongside the existing list. Change the line to:

```
**Ad-hoc skills:**
`/brainstorm` (explore ideas interactively or let Claude analyze autonomously), `/quick-plan` (insert urgent phase), `/phase-feedback` (iterate on delivered work), `/add-security-findings` (security integration), `/drift` (compare codebase against plans)
```
  </action>
  <verify>
Read the updated SKILL.md and verify:
1. The skill reference section contains a /drift entry with description, output, and argument hint
2. The general workflow question section mentions /drift in the ad-hoc skills list
3. No other sections were accidentally modified
  </verify>
  <done>/cst-help SKILL.md lists /drift in both the skill reference output and the general workflow question ad-hoc skills list.</done>
</task>

<task type="auto">
  <name>Task 2: Update workflow-guide.md and troubleshooting.md</name>
  <files>
plugins/claude-super-team/skills/cst-help/references/workflow-guide.md
plugins/claude-super-team/skills/cst-help/references/troubleshooting.md
  </files>
  <action>
**Part A: Update workflow-guide.md**

In `plugins/claude-super-team/skills/cst-help/references/workflow-guide.md`:

1. In "The Sequential Pipeline" section, add after the `/progress` line but before the "### Brainstorming" subsection:

```
### Analysis

```
/drift [N | --all] -> Compare codebase against planning artifacts (.planning/DRIFT-REPORT.md)
                      Extracts claims from SUMMARY.md, CONTEXT.md, PLAN.md
                      Spawns Explore agents to verify claims against actual code
                      Categorizes findings: confirmed drift, potential drift, aligned
```
```

2. In whatever "When to use each skill" or FAQ-style section exists, add an entry:
- "When to check for drift?" -> After executing multiple phases, after long breaks, or before starting a new roadmap cycle. `/drift` verifies that what was planned and reported matches the actual codebase.

**Part B: Update troubleshooting.md**

In `plugins/claude-super-team/skills/cst-help/references/troubleshooting.md`:

1. Add a new "### Drift Detection" subsection (place it after the existing sections, before any general/catchall section). Include these entries:

```
### Drift Detection

#### "No executed phases found"

**Symptom:** `/drift` exits saying no phases have been executed

**Cause:** No SUMMARY.md files exist in any phase directory

**Solution:**
- Run `/execute-phase {N}` to execute planned phases first
- Drift detection requires at least one completed phase to compare against

#### "Too many claims" warning

**Symptom:** `/drift --all` warns about large claim count

**Cause:** Many completed phases generate many claims, consuming significant agent context

**Solution:**
- Run `/drift {N}` for a specific phase instead of `--all`
- Focus on recently completed or suspect phases

#### Drift report shows many "unverifiable" items

**Symptom:** DRIFT-REPORT.md has mostly unverifiable findings

**Cause:** Planning artifacts contain vague claims (e.g., "improved performance") that cannot be verified by file inspection

**Solution:**
- This is informational, not an error
- Future plans should include concrete deliverables in SUMMARY.md
- Focus on the confirmed and potential drift items for action
```

2. If a "When to Use Each Skill" table or section exists, add a row for `/drift`:
   `/drift` -- After executing phases, to verify planning artifacts match actual codebase state. Use after long breaks or before starting new work on a phase.
  </action>
  <verify>
Read workflow-guide.md and verify /drift appears in the pipeline overview with its description and artifact output.

Read troubleshooting.md and verify:
1. A "Drift Detection" subsection exists with at least 3 troubleshooting entries
2. Each entry has Symptom, Cause, and Solution
3. The "When to Use Each Skill" section (if it exists) includes /drift
  </verify>
  <done>workflow-guide.md includes /drift in the pipeline overview and explains when to use it. troubleshooting.md has drift-specific troubleshooting entries covering common issues (no executed phases, too many claims, unverifiable items).</done>
</task>

</tasks>

<verification>
1. /cst-help SKILL.md skill reference includes /drift entry with description and usage
2. workflow-guide.md lists /drift in the pipeline overview
3. troubleshooting.md has a Drift Detection section with 3+ entries
4. All entries are consistent -- description in SKILL.md matches workflow-guide.md
5. No existing content was accidentally removed or modified beyond the additions
</verification>

<success_criteria>
The /cst-help skill and its reference files include /drift in all relevant sections: skill reference listing, workflow pipeline overview, ad-hoc skills mention, and troubleshooting entries. A user running `/cst-help drift` or `/cst-help` skill reference would see /drift with accurate description and usage guidance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-drift-detection/11-02-SUMMARY.md`
</output>
